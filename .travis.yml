language: node_js
node_js:
  - 14
before_deploy:
  - npm install netlify-cli -g
  - npm run build
jobs:
  include:
    - stage: test
      script: npm test
deploy:
  provider: script
  edge: true
  script: netlify deploy --dir=build --prod
  on:
    branch: main
